1) I think we should have a `load last` or `rematch` option that loads the prior configuration, but we should start with defaults.

2) Yes, that's fine. In the setup screen, users should be able to select their color, however.

3) Why shouldn't it support tournament mode? It makes sense to expose all of those flag options in the setup screen.

4) yes to the proposal

5) That's a fair recommendation

6) yes, that's fine.
